#include <tunables/global>

profile nocodb flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  # Capabilities
  file,
  signal,

  # S6-Overlay
  /etc/services.d/** rwix,
  /etc/cont-init.d/** rwix,

  # Bashio
  /usr/lib/bashio/** ix,
  /tmp/** rw,

  # Access to nocodb files
  /data/** rw,
  /opt/** rw,

  # Start new profile for nocodb service
  nocodb cx,

  profile nocodb flags=(attach_disconnected,mediate_deleted) {
    #include <abstractions/base>

    # Receive signals from S6-Overlay
    signal receive,
  }
}